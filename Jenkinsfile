/* Requires the Docker Pipeline plugin */
pipeline {
    agent none 
    stages {
        stage('Build') { 
            agent none
            steps {
                sh 'python --version'
            }
        }
    }
}

