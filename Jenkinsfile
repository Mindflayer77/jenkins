/* Requires the Docker Pipeline plugin */
pipeline {
    agent none 
    stages {
        stage('Build') { 
    
            steps {
                sh 'python --version'
            }
        }
    }
}

